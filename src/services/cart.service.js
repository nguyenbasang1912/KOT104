const addNewProductToCart = async ({ productId, quatity }) => {};

const manageProductQuantities = async ({
  productId,
  preQuantity,
  afterQuantity,
}) => {};

module.exports = {
  addNewProductToCart,
  manageProductQuantities,
};
